# Very basic RPS game.

It uses the `m3-app` component of my (for now private) Material You Expressive web component library.

That is all.

The app is English, because I only have a US keyboard at home and it would look very weird without all the proper Hungarian letters.
